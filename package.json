{
  "name": "academy-portal",
  "private": true,
  "version": "1.0.0",
  "description": "Academy Portal full-stack final project",
  "scripts": {
    "sync:frontend": "powershell -ExecutionPolicy Bypass -File scripts/sync-frontend.ps1",
    "compose:up": "npm run sync:frontend && docker compose up --build",
    "compose:down": "docker compose down",
    "compose:prod": "docker compose -f docker-compose.yml -f docker-compose.prod.yml up --build",
    "verify": "powershell -ExecutionPolicy Bypass -File scripts/verify-submission.ps1",
    "server:dev": "npm --prefix server run dev",
    "server:test": "npm --prefix server test",
    "server:lint": "npm --prefix server run lint",
    "frontend:dev": "npm --prefix FRONT-4 run dev",
    "frontend:build": "npm --prefix FRONT-4 run build"
  }
}
