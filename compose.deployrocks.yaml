# DeployRocks / Kazi.rocks: set "Compose file" to compose.deployrocks.yaml
# OR leave Compose file empty and deploy only the root Dockerfile.
# Do NOT use docker-compose.yml here — it is for local full-stack (db, redis, worker).

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '80:80'
