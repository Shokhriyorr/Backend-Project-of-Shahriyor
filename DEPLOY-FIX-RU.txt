DEPLOYROCKS FIX - CURRENT STATE
===============================

The app must deploy as ONE app from the root Dockerfile.

Do this in DeployRocks:

1. Settings -> Compose file: leave EMPTY.
2. Deploy branch: deployrocks-root-dockerfile.
3. Do not deploy main on DeployRocks right now; main contains mandatory docker-compose.yml for grading.
4. Delete failed split apps if they exist:
   - shokhriyorr-backend-project-of-shahriyor-api
   - shokhriyorr-backend-project-of-shahriyor-worker
   - shokhriyorr-backend-project-of-shahriyor-frontend
5. Keep only the main app:
   - shokhriyorr-backend-project-of-shahriyor
6. Environment live-now values:
   START_WORKERS_IN_API=false
   ENABLE_BACKGROUND_WORKERS=false
   PASSWORD_RESET_TTL_MINUTES=30
7. Leave REDIS_URL unset for now.
8. Retry deploy.

Why:

DeployRocks auto-detected docker-compose.yml and created separate api, worker,
and frontend apps. That mode fails with:

  Network shokhriyorr-backend-project-of-shahriyor-net does not exist

The repository now keeps mandatory local full-stack Docker in docker-compose.yml
on main, and keeps a DeployRocks-safe branch without root compose:
deployrocks-root-dockerfile.

Expected success:

  https://shokhriyorr-backend-project-of-shahriyor.kazi.rocks/health
  -> {"ok":true}

If deploy reaches nginx reload and fails with:

  host not found in upstream "invalid:3000"

that is a shared DeployRocks/Dokku host issue from another app config. Send
DEPLOY-FIX-NOW.txt to DeployRocks support/admin.
