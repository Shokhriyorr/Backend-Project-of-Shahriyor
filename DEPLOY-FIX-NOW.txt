LAST ERROR FIXED: Dockerfile line 73 had spaces in ENV (build failed).

NOW:
1. Click Retry deploy on DeployRocks (pulls latest GitHub main).
2. After Live, open Environment and paste deployrocks.env.local.
3. Set PASSWORD_RESET_TTL_MINUTES = 30  (delete the long random value if present).
4. Retry deploy again.

SECURITY: Your GitHub token appeared in deploy logs. Revoke it:
  GitHub -> Settings -> Developer settings -> Personal access tokens -> revoke old token.
